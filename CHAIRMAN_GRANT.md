# Chairman Grant — Consolidation Sprint Authority

**Granted to:** the Integration Lead (Claude Dispatch) operating on host `ai-stack`.
**Granted by:** the Chairman, via the Enclave Master Directive (self-contained, §2).
**Recorded:** 2026-07-14.

## Scope
Full, blanket, cross-directory pre-approval to act, decide, execute commands, and override
*project-level* guardrails on the ai-stack VM, **for the duration of the Enclave
consolidation sprint only**.

- `~/enclave` is the primary working root (full authority).
- `~/local-stack` and `~/corp-os` are SOURCES: read freely; write only surgical, listed
  changes (e.g., the credential rotation; corp-os v1.1 application).

## Reserved powers (Chairman confirmation still required)
- Destructive infrastructure: VM/storage/network deletion.
- Pushing anything containing secrets (never permitted at all).
- Account-level GitHub operations: repo deletion, archiving, visibility changes.
- All public posting (LinkedIn/HN/social) and network outreach.
- Demo recording sign-off; GPU-pass acceptance of large-model behavior.

## Revocation
This grant **revokes automatically when the product ships** (launch gate of the Master
Directive passes and the launch is executed). After revocation, deployed Enclave CEOs and
any builder agents return to per-project jurisdiction under corp-os governance.

## Audit trail
Session-by-session record: dated handoffs in `local-stack/memory/decisions.md` (private
instance) — status lives on the board, never in recall.
